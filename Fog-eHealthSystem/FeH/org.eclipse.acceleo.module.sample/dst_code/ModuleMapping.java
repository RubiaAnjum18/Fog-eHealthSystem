		public class ModuleMapping{
ModuleMapping moduleMaping = 
ModuleMapping.createModuleMapping();

 if(CLOUD){ 
moduleMapping.addModuleToDevice("connecter", "cloud);
moduleMapping.addModuleToDevice("concentartion_calculator","cloud");
for(FogDevice device : fogDevices){ 
    if(device.getName().startsWith("m")){

moduleMapping.addModuleToDevice("patient",
getName());

 } 
      }
 } else{
    moduleMapping.addModuleToDevice("connecter","cloud");
